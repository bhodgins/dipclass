defmodule Window do
  @moduledoc """
  Create windows / Kernels of images
  """

  defstruct x1: 0, y1: 0,
            x2: 0, y2: 0,
            pixels: []

  def grab(image, x1, y1, x2, y2) do

  end

  def set(image, window, x1, y1) do

  end
end
